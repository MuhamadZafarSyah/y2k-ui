---
title: Tooltip
description: A Y2K flat tooltip with thick navy border for hints and labels.
---

# Tooltip

A small flat tooltip — white fill, thick navy border. Wrap your app in
`TooltipProvider` once at the root.

<ComponentPreview
  name="tooltip-demo"
  preview={<TooltipDemo />}
  source={`import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "@/components/ui/tooltip"

<TooltipProvider>
  <Tooltip>
    <TooltipTrigger>Hover me</TooltipTrigger>
    <TooltipContent>kawaii tip ✨</TooltipContent>
  </Tooltip>
</TooltipProvider>`}
/>

## Installation

<CodeBlock language="bash" filename="terminal.sh" code="npx y2kui@latest add tooltip" />

## Usage

```tsx
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "@/components/ui/tooltip"
```

## Props

<PropsTable
  data={[
    { prop: "open", type: "boolean", description: "Controlled open state." },
    { prop: "defaultOpen", type: "boolean", description: "Initial open state (uncontrolled)." },
    { prop: "delayDuration", type: "number", default: "100", description: "Delay in ms before the tooltip opens on hover." },
    { prop: "sideOffset", type: "number", default: "6", description: "Distance between the trigger and the tooltip." },
  ]}
/>
