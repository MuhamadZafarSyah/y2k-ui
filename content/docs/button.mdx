---
title: Button
description: A Y2K flat button component with thick navy outline and pastel fill. Supports loading states, leading/trailing icons, and 6 color variants. Built on Radix UI.
---

# Button

A flat button. Thick navy outline, pastel fill, six color variants, and a
snappy press feel. Supports `loading`, `leadingIcon`, `trailingIcon`, and
`asChild` for composing with other elements.

## Variants

<ComponentPreview
  name="button-basic-demo"
  preview={<ButtonBasicDemo />}
  source={`import { Button } from "@/components/ui/button"

<Button>Default</Button>
<Button variant="pink">Pink</Button>
<Button variant="mint">Mint</Button>
<Button variant="lemon">Lemon</Button>
<Button variant="blue">Blue</Button>
<Button variant="lilac">Lilac</Button>`}
/>

## With icons

Pass any React node through `leadingIcon` or `trailingIcon`.

<ComponentPreview
  name="button-icons-demo"
  preview={<ButtonIconsDemo />}
  source={`import { Button } from "@/components/ui/button"
import { PlusIcon, ArrowRightIcon, HeartIcon, StarIcon, DownloadIcon, SendIcon } from "lucide-react"

<Button variant="mint" leadingIcon={<PlusIcon />}>New file</Button>
<Button variant="blue" trailingIcon={<ArrowRightIcon />}>Continue</Button>
<Button variant="pink" leadingIcon={<HeartIcon />}>Like</Button>
<Button variant="lemon" trailingIcon={<StarIcon />}>Star</Button>
<Button variant="outline" leadingIcon={<DownloadIcon />}>Download</Button>
<Button variant="ghost" leadingIcon={<SendIcon />}>Send</Button>`}
/>

## Loading state

When `loading` is `true`, the button becomes disabled, swaps the children for
a spinner, and sets `aria-busy`. Use `loadingText` to keep a label visible.

<ComponentPreview
  name="button-loader-demo"
  preview={<ButtonLoaderDemo />}
  source={`import { Button } from "@/components/ui/button"
import { CheckIcon, DownloadIcon } from "lucide-react"

const [loading, setLoading] = React.useState(false)

<Button
  loading={loading}
  onClick={() => setLoading(true)}
  leadingIcon={<CheckIcon />}
>
  Save changes
</Button>

<Button
  loading={loading}
  loadingText="Saving…"
  variant="mint"
  onClick={() => setLoading(true)}
>
  Save
</Button>

<Button
  loading={loading}
  loadingText="Uploading"
  variant="blue"
  onClick={() => setLoading(true)}
  trailingIcon={<DownloadIcon />}
>
  Upload
</Button>`}
/>

## Installation

<CodeBlock
  language="bash"
  filename="terminal.sh"
  code="npx y2kui@latest add button"
/>

## Usage

```tsx
import { Button } from "@/components/ui/button";

<Button onClick={() => console.log("clicked")}>Save</Button>;
```

## Props

<PropsTable
  data={[
    {
      prop: "variant",
      type: "'default' | 'pink' | 'blue' | 'mint' | 'lilac' | 'lemon' | 'outline' | 'secondary' | 'ghost' | 'destructive' | 'link'",
      default: "'default'",
      description: "Pastel color style for the button.",
    },
    {
      prop: "size",
      type: "'xs' | 'sm' | 'default' | 'lg' | 'icon' | 'icon-xs' | 'icon-sm' | 'icon-lg'",
      default: "'default'",
      description: "Visual size of the button.",
    },
    {
      prop: "loading",
      type: "boolean",
      default: "false",
      description: "Show a spinner, disable the button, and set aria-busy.",
    },
    {
      prop: "loadingText",
      type: "string",
      description: "Optional label shown while loading (defaults to children).",
    },
    {
      prop: "leadingIcon",
      type: "ReactNode",
      description: "Icon rendered before the children.",
    },
    {
      prop: "trailingIcon",
      type: "ReactNode",
      description: "Icon rendered after the children.",
    },
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Render as a Radix Slot so you can compose with your own element.",
    },
    {
      prop: "disabled",
      type: "boolean",
      default: "false",
      description: "Disable the button (also set automatically when loading).",
    },
  ]}
/>
